# Required API Keys (server-side only — NEVER commit .env with real values)
HELIUS_KEY=your-helius-api-key
JUP_API_KEY=your-jupiter-api-key

# Server config
PORT=8080

# Optional: restrict CORS to specific origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Legacy x402 payment config (optional)
# PAY_TO_ADDRESS=YOUR_WALLET_ADDRESS
# PAYMENT_NETWORK=eip155:84532
# FACILITATOR_URL=https://x402.org/facilitator
